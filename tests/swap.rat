[* This program swaps two numbers *]
%%
    int a, b;
    get(a, b);
    if (a /= b)
    {
        int temp; [* For extra storage *]
        temp = a;
        a = b;
        b = temp;
    }
    fi
    put(a, b); [* whoo hoo swap! *]
%%
